[pytest]
# Self-contained: run `python -m pytest` from this directory. The method's tests
# live inside the package (src/memstrata/tests + src/memstrata/skills/*/tests) and
# import `memstrata` absolutely. They must not import the bench (vmem_bench).
pythonpath = src
testpaths = src/memstrata
